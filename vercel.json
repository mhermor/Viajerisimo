{
  "buildCommand": "cd frontend && npm install && npm build --configuration=production --no-ssr",
  "outputDirectory": "frontend/dist/viajerisimo/browser",
  "installCommand": "echo 'skip root install'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.html" }
  ]
}
