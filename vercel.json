{
  "buildCommand": "cd frontend && npm install && npm run build -- --no-ssr",
  "outputDirectory": "frontend/dist/viajerisimo/browser",
  "installCommand": "echo 'skip root install'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
